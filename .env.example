# MongoDB Docker 설정
MONGO_URL=mongodb://wzuser:wzpassword@localhost:27017/wzdata
DB_NAME=wzdata

# MongoDB 관리자 계정 (Docker Compose에서 사용)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123

# 애플리케이션 MongoDB 사용자
MONGO_USERNAME=wzuser
MONGO_PASSWORD=wzpassword

# 로그 레벨
LOG_LEVEL=info

# JSON 파일 경로
JSON_DIRECTORY=./string.wz
